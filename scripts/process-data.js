import fs from 'fs';import path from 'path';const csvToJSON = (csv) => {  const lines = csv.split('\n');  const result = [];  const headers = lines[0].split(',');  for (let i = 1; i < lines.length; i++) {    const obj = {};    const currentline = lines[i].split(',');    for (let j = 0; j < headers.length; j++) {      obj[headers[j]] = currentline[j];    }    result.push(obj);  }  return result;};const processData = async () => {  try {    const safeCsv = fs.readFileSync(path.resolve('./public/data/safe.csv'), 'utf-8');    const toxicCsv = fs.readFileSync(path.resolve('./public/data/toxic.csv'), 'utf-8');    const safeJson = csvToJSON(safeCsv);    const toxicJson = csvToJSON(toxicCsv);    fs.writeFileSync(path.resolve('./src/data/safe.json'), JSON.stringify(safeJson, null, 2));    fs.writeFileSync(path.resolve('./src/data/toxic.json'), JSON.stringify(toxicJson, null, 2));    console.log('Successfully converted CSV to JSON');  } catch (error) {    console.error('Error processing data:', error);  }};processData();